$(document).ready(function(){let
btnMenu=$('.header-menu'),headerNav=$('.header-nav'),btnAbout=$('.friends-pet__button'),overlay=$('.overlay'),popup=$('.popup'),closeBtn=$('.close'),popupImg=$('.popup__img'),btnTab=$('.tab[data-tab]');$(btnMenu).on('click',function(){$(this).toggleClass('header-menu_active');$(headerNav).toggleClass('header-nav_active')});$('.header-nav__link_prevent-default').on('click',function(event){event.preventDefault();let target=$(this).attr('href');let top=$(target).offset().top;$(headerNav).removeClass('header-nav_active');$(btnMenu).removeClass('header-menu_active');$('html, body').animate({scrollTop:top},800)});$(btnTab).on('click',function(event){event.preventDefault();let attr=$(this).attr('data-tab');$('.page').removeClass('page_active');$('.page[data-tab='+attr+']').toggleClass('page_active')});$(btnAbout).on('click',function(event){event.preventDefault();let name=$(this).prev('.friends-pet__title').text(),parentBtn=$(this).parent(''),img=parentBtn.prev('.friends-pet__img');$('.popup__title').text(name);popupImg.attr('src',img.attr('src'));overlay.toggleClass('overlay_active');popup.toggleClass('popup_active');console.log(name)});$(closeBtn).on('click',function(event){event.preventDefault();overlay.toggleClass('overlay_active');popup.toggleClass('popup_active')})})